import 'package:flutter/material.dart';

class ahades extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
        child: Text('Ahades',style: TextStyle(fontWeight: FontWeight.bold , fontSize: 40),),
      ),

    );
  }
}